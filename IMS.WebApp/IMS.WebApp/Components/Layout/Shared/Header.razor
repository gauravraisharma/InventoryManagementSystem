<MudAppBar Color="Color.Primary" Elevation="2">
    <div id="nav-belt" class="d-flex align-items-center justify-content-between w-100">
        <!-- Left Section -->
        <div class="nav-left d-flex align-items-center">
            <MudText Typo="Typo.h6" Class="ml-2">tackingStuff.in</MudText>
            <MudText Typo="Typo.body1" Class="ml-2">Delivering to Dehradun 248001</MudText>
        </div>

        <!-- Center Section -->
        <div class="nav-fill flex-grow-1 mx-4">
            <MudTextField T="string" Placeholder="Search Amazon.in" Class="w-100" />
        </div>

        <!-- Right Section -->
        <div class="nav-right d-flex align-items-center">
            <MudText Typo="Typo.body1" Class="ml-4">Hello, sign in</MudText>
            <MudText Typo="Typo.body1" Class="ml-4">Cart (0)</MudText>
        </div>
    </div>
</MudAppBar>

<style>
    #nav-belt {
        display: flex;
        align-items: center;
        justify-content: space-between;
        width: 100%;
        height: 60px;
        font-size: 14px;
        position: relative;
        z-index: 5;
    }

        #nav-belt .nav-left {
            display: flex;
            align-items: center;
            justify-content: flex-start;
            flex: 0 1 auto; /* Keeps it to the left and prevents it from growing */
        }

        #nav-belt .nav-fill {
            display: flex;
            align-items: center;
            justify-content: center;
            flex: 1 1 auto; /* Allows it to grow and take up the central space */
            margin: 0 16px; /* Adds some spacing between left and right sections */
        }

        #nav-belt .nav-right {
            display: flex;
            align-items: center;
            justify-content: flex-end;
            flex: 0 1 auto; /* Keeps it to the right and prevents it from growing */
        }

</style>